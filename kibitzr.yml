- name: Release
  script: git pull
  transform:
    - python: |
        if content.strip() == 'Already up-to-date.':
          content = ''
  notify:
    - bash: |
        yarn
        npx prisma migrate deploy
        yarn build
        pm2 reload urc_ms